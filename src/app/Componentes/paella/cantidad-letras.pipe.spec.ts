import { CantidadLetrasPipe } from './cantidad-letras.pipe';

describe('CantidadLetrasPipe', () => {
  it('create an instance', () => {
    const pipe = new CantidadLetrasPipe();
    expect(pipe).toBeTruthy();
  });
});
